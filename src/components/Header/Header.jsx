import React from 'react';
import { Header as Container, Title } from './Header.style';

function Header() {
  return (
    <Container>
      <Title>Angular / Angular-cli</Title>
    </Container>
  );
}

export default Header;
