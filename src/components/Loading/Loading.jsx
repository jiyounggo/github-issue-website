import React from 'react';
import { Container, LoadingSpinner } from './Loading.style';

const Loading = () => {
  return (
    <Container>
      <LoadingSpinner />
    </Container>
  );
};

export default Loading;
